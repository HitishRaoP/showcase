{
  "scripts": {
    "build": "bunx @tailwindcss/cli -i ./styles/input.css -o ./styles/tailwind.css --minify"
  },
  "devDependencies": {
    "@tailwindcss/cli": "^4.0.0",
    "tailwindcss": "^4.0.0"
  }
}
